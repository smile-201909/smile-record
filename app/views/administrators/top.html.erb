<h2>管理者ページ</h2>
<h4>売上確認</h4>

<p>日別売上：<%= @day_total_price %>円</p><br>
<p>月別売上：<%= @month_total_price %>円</p><br>
<p>年別売上：<%= @year_total_price %>円</p><br>
<p>総売上：<%= @total_price %>円</p>


<h4>購入履歴一覧</h4>
<p>新着順▼</p>
<% @receipts.each do |receipt| %>
  <p>注文日：<%= receipt.created_at %></p>
  <p>注文者：<%= receipt.purchaser_family_name%><%= receipt.purchaser_family_name %></p>
  <p>
    配送ステータス：
    <select>
      <option>受付▼</option>
      <option>ただいま準備中▼</option>
      <option>配送済み▼</option>
    </select>
  </p>
  <% @receipt_items.each do |item| %>
    <%= attachment_image_tag item, :product_image_id, format: 'jpeg', fallback: 'no_image_jpg', size: '40x40' %>
    <%= item.product_name %>
    <p>数量：<%= item.product_amount %>枚</p>
    <p>価格：<%= item.price %></p>
  <% end %>
  <p>送付先</p>
  <%= receipt.delivery_address %>
  <p>決済方法</p>
  <%= receipt.settlement %>
  <p>小計：　<%= %></p>
  <p>消費税：</p>
  <p>送料：  500円</p>
  <p>購入価格総計：<%= receipt.total_price %></p>

<% end %>
