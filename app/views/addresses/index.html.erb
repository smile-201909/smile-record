<table>
  <thead>
    <tr>
      <th>姓（漢字）</th>
      <th>名（漢字）</th>
      <th>姓（カナ）</th>
      <th>名（カナ）</th>
      <th>郵便番号</th>
      <th>住所</th>
      <th>電話番号</th>
      <th colspan="3"></th>
    </tr>
  </thead>


  <tbody>
    <% if @user.present? %>
    <% @user.addresses.each do |f| %>
    <tr>
      <td><%= f.family_name %></td>
      <td><%= f.first_name %></td>
      <td><%= f.family_name_kana %></td>
      <td><%= f.first_name_kana %></td>
      <td><%= f.post %></td>
      <td><%= f.a_address %></td>
      <td><%= f.phone %></td>
      <td><%#= link_to 'Edit', edit_address_path(address) %></td>
      <td><%#= link_to 'Destroy', address, method: :delete, data: { confirm: '削除しますか？' }%></td>
    </tr>
    <% end %>
    <% end %>
  </tbody>
</table>
